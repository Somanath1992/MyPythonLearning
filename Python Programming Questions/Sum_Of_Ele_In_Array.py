# Find Out Sum of All Elements in Array / List
arr = [1, 2, 3, 4, 5]
print(sum(arr))
# Add sum value in existing sum of elements
print(sum(arr, 10))
# Reduce sum value in existing sum of elements
print(sum(arr, -10))
